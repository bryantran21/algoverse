# Pin transformers new enough for Qwen2.5-VL. On Kaggle/Colab, torch is preinstalled
# with CUDA — do NOT reinstall torch there, just the rest.
transformers>=4.49.0
accelerate>=0.34.0
datasets>=2.19.0
qwen-vl-utils>=0.0.8
typst>=0.11.0
Pillow>=10.0.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
tqdm>=4.66.0
numpy>=1.26.0
# Only needed if LOAD_IN_4BIT=1 (single-T4 fallback):
bitsandbytes>=0.43.0 ; platform_system == "Linux"
